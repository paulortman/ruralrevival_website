services:
  build:
    image: klakegg/hugo:0.111.3
    volumes:
      - ".:/src"
  server:
    image: klakegg/hugo:0.111.3
    command: server -DF --disableFastRender --watch --poll 700ms
    volumes:
      - ".:/src"
    ports:
      - "1313:1313"